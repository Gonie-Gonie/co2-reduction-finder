## Changes

- Promoted the corrected ANN H5 model set into the official repository model registry.
- Added a registry-driven model framework for building types, model segment weights, ANN input/output specs, and future H5/Keras model updates.
- Added rigorous Excel transfer audit notes covering stratified cached workbook comparisons across energy/stat outputs and retrofit costs.
- Moved retrofit unit costs and indirect-cost rate factors into `assets/retrofit_costs.json` so cost assumptions can be reviewed and updated separately from Rust code.
- Documented which remaining reference files were promoted into official assets and which were one-time transfer artifacts.

## Notes

- Version bumped to `0.1.8` for the matching GitHub Release tag.
- The app still embeds compact runtime assets; source H5/Keras files remain in the repo for traceability and regeneration.
