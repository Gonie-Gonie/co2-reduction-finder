## Changes

- Reworked the dashboard into graph-based sections that mirror the Excel `MAIN` sheet flow:
  - user-entered retrofit alternatives
  - single element-technology effects
  - Pareto result candidates
- Added selectable result metrics using the Excel `계산sheet` conversion factors:
  - electricity
  - gas
  - final energy demand
  - primary energy demand
  - greenhouse gas emissions
- Added mean/std result handling so the app can draw distribution curves linked to the same final dashboard logic as Excel.
- Changed normal user-entered and single-measure results to update live as inputs change.
- Kept Pareto as an explicit backend job, while preserving the Pareto result when only user comparison options change.
- Changed Pareto scoring from fixed CO2-only dominance to the currently selected metric's reduction/cost front.
- Changed GitHub Release titles to use the tag only, avoiding duplicate-looking product/version headings.

## Notes

- The metric factors are centralized in `src/domain/metrics.rs` and match Excel `계산sheet!R5:U9`.
- Excel chart links checked from the workbook XML:
  - `chart2`: distribution curves from `계산sheet!H56:DL63`
  - `chart4`: single-measure effects from `계산sheet!B69:J80`
  - `chart1`/`chart3`: reduction-cost scatter data from `계산sheet!H86:J285`
